import 'package:flutter/material.dart';

class CashBuyer extends StatelessWidget {
  const CashBuyer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cash Buyer'),
      ),
    );
  }
}
